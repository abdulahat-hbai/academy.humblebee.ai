---

---

<section></section>

<!-- ---
interface Props {
  step: number;
  title: string;
  description: string;
  icon: string;
}
const {step, title, description, icon} = Astro.props;
---
<div class="bg-foreground text-white rounded-xl p-4 flex flex-col">
  <div class="flex items-center space-x-4">
    <div class="flex items-center justify-center rounded-full">
      <div {title} class=""/>
    </div>
    <h3 class="text-xl font-semibold font-heading">{step} {title}</h3>
  </div>
  <p class="m-6">{description}</p>
  <img src={icon} class="w-60 mx-auto">
  <button class="mt-6 bg-brand hover:bg-brand-hover text-brand-foreground font-sans py-2 px-4">
    Learn more →
  </button>
</div> -->
